---
title: "v0.11 Wave 3.1 — Testcases"
type: test-checklist
version: "0.11-w3.1"
date: 2026-04-26
author: MPO
---

# v0.11 Wave 3.1 — Testcases

> Manuelle Abnahme-Checkliste fuer alle 23 Bugfixes + 4 Feature-Requests.
> Vor dem Test: `npm run dist` und gepackte App starten.

## Vorbereitung

1. App komplett beenden (alle Prozesse)
2. `cd /Users/Shared/Nextcloud/Claude/ClaudeCode01/cipher-mux-electron`
3. `npm run build` — muss sauber durchlaufen
4. `npm test` — alle Tests muessen PASS (erwartet: 586+)
5. `npm run dist` — DMG/App bauen
6. App aus `/dist` starten

---

## SP-1: Session Lifecycle (4 Tests)

### T-SL.1 — Kein --resume bei Session-Start
- [ ] Orchestrator/MPO/Companion-Session starten
- [ ] In tmux pruefen: `tmux capture-pane -t <session> -p | grep resume` → leer
- **PASS wenn:** Kein `--resume` im Claude-CLI-Aufruf

### T-SL.2 — Beendete Session gibt Zelle frei
- [ ] Session im Grid starten
- [ ] In der Session `exit` oder `/exit` eingeben
- [ ] Warten 5-10s
- **PASS wenn:** Zelle wird als leer angezeigt, neue Session startbar

### T-SL.3 — Session-Restore-Dialog nach Neustart
- [ ] 2+ Sessions im Grid starten
- [ ] App beenden (Cmd+Q)
- [ ] App neu starten
- **PASS wenn:** Restore-Dialog erscheint ("Sessions wiederherstellen?")

### T-SL.4 — Restore: Ja → Sessions im Grid
- [ ] Im Restore-Dialog "Ja" klicken
- **PASS wenn:** Sessions erscheinen im Grid an ihren alten Positionen

---

## SP-2: MCP + Companion Infrastructure (4 Tests)

### T-MC.1 — Companion hat MCP-Tools
- [ ] Companion-Session starten
- [ ] In Companion eingeben: "Welche MCP-Tools hast du verfuegbar?"
- **PASS wenn:** Companion listet mux_send, mux_read, notes-Tools

### T-MC.2 — Companion kann mux_send nutzen
- [ ] In Companion: "Sende eine Testnachricht via mux_send an topic chat"
- **PASS wenn:** Nachricht erscheint im Message-Bus

### T-MC.3 — MPO hat eigene CLAUDE.md
- [ ] MPO-Session starten
- [ ] Pruefen: `cat ~/.config/cipher-mux/mpo/CLAUDE.md` → enthaelt MPO-Rolle
- **PASS wenn:** CLAUDE.md enthaelt MPO-spezifische Instruktionen, nicht Companion

### T-MC.4 — Orchestrator hat eigene CLAUDE.md
- [ ] Orchestrator-Session starten
- [ ] Pruefen: `cat ~/.config/cipher-mux/orchestrator/CLAUDE.md` → enthaelt Orchestrator-Rolle
- **PASS wenn:** CLAUDE.md enthaelt Orchestrator-spezifische Instruktionen

---

## SP-3: LauncherCell + Grid UX (7 Tests)

### T-LC.1 — LauncherCell Popup bei leerer Zelle
- [ ] Leere Grid-Zelle klicken
- **PASS wenn:** Unified-Popup erscheint (Presets: Orchestrator, MPO, Companion, Custom)

### T-LC.2 — Popup als Overlay (nicht in-cell)
- [ ] Leere Zelle klicken → Popup oeffnet sich
- **PASS wenn:** Popup schwebt als Overlay ueber dem Grid, nicht abgeschnitten in Zelle

### T-LC.3 — Session starten via Preset
- [ ] Preset waehlen (z.B. Companion) → ggf. Pfad bestaetigen
- **PASS wenn:** Session startet an der geklickten Position

### T-LC.4 — Overlay schliesst nach Platzierung
- [ ] Session via LauncherCell starten
- **PASS wenn:** Popup/Overlay schliesst sich automatisch nach Start

### T-LC.5 — 5x hintereinander platzieren (kein Haengen)
- [ ] 5 Sessions nacheinander in leere Zellen starten
- **PASS wenn:** Kein Overlay-Haengen, App bleibt responsive

### T-LC.6 — Doppelzellen-Placement korrekt
- [ ] Grid auf 2x2+ stellen, eine Zelle mergen (Doppelzelle)
- [ ] Session in Nachbarzelle platzieren
- **PASS wenn:** Session landet an korrekter Position, nicht hinter Doppelzelle

### T-LC.7 — Terminal-Zeilen stabil nach Resize
- [ ] Session im Grid laufen lassen
- [ ] Grid-Groesse aendern (Fenster resizen)
- **PASS wenn:** Terminal-Zeilen bleiben intakt, kein Auseinanderfallen

---

## SP-4: Voice COM (7 Tests)

### T-VC.1 — COM startet Voice-Relay im Hintergrund
- [ ] StatusBar: COM-Button klicken
- **PASS wenn:** Voice-Relay startet automatisch (kein Grid-Selektor), Sidebar zeigt lila Glow

### T-VC.2 — STT-Text wird mit Enter gesendet
- [ ] COM aktiv, sprechen
- **PASS wenn:** Text erscheint in Voice-Relay UND Claude beginnt zu antworten

### T-VC.3 — TTS-Antwort (best-effort)
- [ ] Claude antwortet in Voice-Relay
- **PASS wenn:** TTS spielt Antwort ab (abhaengig von Piper-Setup)

### T-VC.4 — STT pausiert bei BugReport
- [ ] STT aktiv (OFF→STT)
- [ ] BugReport oeffnen
- **PASS wenn:** STT pausiert, kein Text in BugReport-Bubbles

### T-VC.5 — STT resumed nach BugReport-Close
- [ ] BugReport schliessen
- **PASS wenn:** STT ist wieder aktiv

### T-VC.6 — Kein Voice-Preset in LauncherCell
- [ ] Leere Grid-Zelle klicken
- **PASS wenn:** Kein "Voice" oder "Voice-Relay" in der Preset-Liste

### T-VC.7 — Graceful Shutdown bei COM→OFF
- [ ] COM aktiv, dann OFF klicken
- **PASS wenn:** Voice-Relay-Session wird beendet (nicht sofort gekillt, kurzer Delay OK)

---

## SP-5: UI Polish + Sidebar (12 Tests)

### T-UI.1 — Message-Bus in Sidebar sichtbar
- [ ] Sidebar oeffnen
- **PASS wenn:** Nachrichten-Bereich sichtbar, zeigt Messages (Topics: chat/status/system)

### T-UI.2 — Info-Popup Tab-Reihenfolge
- [ ] Info-Popup oeffnen (Cmd+,)
- **PASS wenn:** Settings = erste Seite, "Ueber" statt "Features", Shortcuts rechts

### T-UI.3 — Workspace-Popup kbd-Tags gerendert
- [ ] Workspace-Popup oeffnen
- **PASS wenn:** Tastaturkuerzel als gerenderte kbd-Tags (nicht Rohtext `<kbd>`)

### T-UI.4 — Default-Workspace setzbar
- [ ] Workspace-Popup → Workspace als Default markieren
- **PASS wenn:** Default-Toggle/Button vorhanden und funktional

### T-UI.5 — Sidebar responsive (ausgepoppt)
- [ ] Sidebar auspoppen (separates Fenster)
- [ ] Fenster groesser ziehen
- **PASS wenn:** Inhalt fuellt verfuegbaren Platz, kein fixer 280px-Bereich

### T-UI.6 — Theme-Wechsel auf Sidebar-Fenster
- [ ] Sidebar auspoppen
- [ ] Theme wechseln (z.B. Dark→Light)
- **PASS wenn:** Sidebar-Popup-Fenster uebernimmt neues Theme

### T-UI.7 — Notes-Button erkennbar
- [ ] Notes-Bereich oeffnen
- **PASS wenn:** Button hat Hover-Effekt, gestrichelte Border, klar als Button erkennbar

### T-UI.8 — Entity-Farben in Session-Headern
- [ ] Orchestrator, MPO, Companion starten
- **PASS wenn:** Jeder Session-Header zeigt rollenspezifische Farbe

### T-UI.9 — Context-%-Anzeige plausibel
- [ ] Session laenger nutzen (mehrere Prompts)
- [ ] Context-% in Session-Header beobachten
- **PASS wenn:** Wert steigt ueber 30% bei intensiver Nutzung, spiegelt realen Verbrauch

### T-UI.10 — Notes-Filter
- [ ] Mehrere Notes erstellen
- [ ] Filter-Input nutzen
- **PASS wenn:** Notes werden nach Suchtext gefiltert

### T-UI.11 — Session-Anzeigename = Verzeichnisname
- [ ] Hintergrund-Sessions oder verwaiste Sessions anschauen
- **PASS wenn:** Name zeigt Verzeichnisname (z.B. "companion"), nicht Pfad oder cmux-ID

### T-UI.12 — Theme auf Workspaces-Fenster
- [ ] Workspaces-Fenster oeffnen
- [ ] Theme wechseln
- **PASS wenn:** Workspaces-Fenster uebernimmt neues Theme

---

## Cross-Cutting (3 Tests)

### T-CC.1 — npm test
- [ ] `npm test` ausfuehren
- **PASS wenn:** 586+ Tests, 0 Failures

### T-CC.2 — npm run build
- [ ] `npm run build` ausfuehren
- **PASS wenn:** Sauber, keine Fehler (Chunk-Size-Warning OK)

### T-CC.3 — npm run dist
- [ ] Alle cipher-mux Prozesse beenden
- [ ] `npm run dist` ausfuehren
- **PASS wenn:** DMG/App wird erstellt ohne Fehler

---

## Zusammenfassung

| Kategorie | Tests | Kritisch |
|-----------|-------|----------|
| Session Lifecycle | 4 | T-SL.2, T-SL.3 |
| MCP/Companion | 4 | T-MC.1 |
| LauncherCell + Grid | 7 | T-LC.1, T-LC.3 |
| Voice COM | 7 | T-VC.1, T-VC.2 |
| UI Polish | 12 | T-UI.1, T-UI.8, T-UI.9 |
| Cross-Cutting | 3 | T-CC.1, T-CC.3 |
| **Gesamt** | **37** | |
