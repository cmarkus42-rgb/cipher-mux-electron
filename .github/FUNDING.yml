# Funding options for cipher-mux.
# Uncomment / fill in when a channel is actually set up — empty or broken
# links look worse than no sponsor button at all.

# github: cmarkus42           # GitHub Sponsors profile
# ko_fi: cmarkus42            # Ko-fi profile
# custom:                     # Any custom URLs (PayPal.me, Liberapay, IBAN landing page)
#   - "https://example.com/support"
